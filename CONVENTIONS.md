<div dir="rtl">

# قراردادهای پروژه (Conventions)

این فایل تصمیم‌های معماری و قراردادهایی رو مستند می‌کنه که توی خود کد به‌وضوح دیده نمی‌شن، ولی رعایتشون برای یکنواختی و خوانایی پروژه لازمه. هدف اینه که هم خودت بعداً یادت بیاد چرا این تصمیم‌ها رو گرفتی، هم هر کس دیگه‌ای که کد رو می‌خونه سریع بفهمه ساختار چطوریه.

## ۱. قرارداد توابع لایه‌ی دیتابیس

هر تابعی که توی لایه‌ی دیتابیس (فایل مسئول ارتباط با DB) نوشته می‌شه، از یکی از این دو الگو پیروی می‌کنه. تصمیم بین این دو، بر اساس **نیاز واقعی صداکننده به داده** گرفته می‌شه، نه صرفاً بر اساس نوع عملیات (`get`/`create`/`update`/`delete`).

**الف) توابعی که صداکننده به داده‌ی برگشتی نیاز داره**

</div>

<div dir="ltr">

```
return (success: bool, message: str, data)
```

</div>

<div dir="rtl">

- `success` → آیا عملیات موفق بود یا نه
- `message` → پیام قابل‌نمایش (برای flash یا لاگ)
- `data` → مقدار واقعی برگشتی. اگه چند مقدار لازم بود برگردونده بشه (مثلاً هم `user` هم `posts`)، `data` یک dict می‌شه، نه چند مقدار جدا:

</div>

<div dir="ltr">

```python
return True, "message", {"user": user, "posts": posts}
```

</div>

<div dir="rtl">

توابع `get` معمولاً همیشه توی این دسته قرار می‌گیرن، چون کل هدفشون برگردوندن داده‌ست. `create` هم وقتی صداکننده به داده‌ی تازه‌ساخته نیاز داره (مثلاً برای نمایش فوری یا استفاده‌ی بعدی از `id`)، توی همین دسته می‌ره.

**ب) توابعی که صداکننده فقط نیاز داره بدونه عملیات موفق بوده یا نه**

</div>

<div dir="ltr">

```
return (success: bool, message: str)
```

</div>

<div dir="rtl">

این حالت **پیش‌فرض** برای `create`, `update`, `delete` هست — مگه اینکه نیاز مشخصی به داده‌ی برگشتی وجود داشته باشه (طبق حالت الف). یعنی این توابع لزوماً به‌خاطر نوعشون سه‌تایی نمی‌شن؛ فقط وقتی سه‌تایی می‌شن که واقعاً جایی از داده‌ی برگشتی استفاده بشه.

**قانون کلی:** هیچ‌وقت تعداد مقادیر برگشتی تابع رو متغیر یا حدسی نذار (نه ۴تایی، نه ۵تایی) — همیشه یا دوتایی یا سه‌تایی، طبق قانون بالا. اگه دیتای بیشتری لازم شد، از dict استفاده کن نه مقادیر اضافه‌ی جدا.

## ۲. مدیریت خطا در لایه‌ی دیتابیس

- هر تابع نوشتاری (create/update/delete) باید عملیاتش رو داخل `try/except` بگیره.
- توی `except`، همیشه `session.rollback()` صدا زده بشه (حتی برای عملیات خواندنی، چون اگه session وسط کار به خطا بخوره، ممکنه توی حالت نامعتبر بمونه).
- پیام خطای کامل (`str(e)`) هیچ‌وقت مستقیم به کاربر (flash) نمایش داده نمی‌شه — فقط برای دیباگ (مثلاً `print` یا `logging`) استفاده می‌شه. به کاربر همیشه یک پیام عمومی و امن نشون داده می‌شه.

## ۳. کنترل دسترسی و مالکیت داده (Ownership)

هر جا که یک کاربر قراره فقط به داده‌ی خودش دسترسی داشته باشه (نه داده‌ی کاربرهای دیگه):

- شناسه‌ی کاربر (`user_id`) همیشه از `session` خونده می‌شه، نه از `request.form` یا هر ورودی دیگه‌ای که از سمت کاربر میاد.
- توی خود query، هم شناسه‌ی رکورد (مثل `post_id`) هم `user_id` با هم فیلتر می‌شن، تا مطمئن بشیم اون رکورد واقعاً متعلق به همون کاربره.
- مخفی کردن id توی فرم (hidden input) به‌تنهایی کافی نیست و یک لایه‌ی امنیتی واقعی حساب نمی‌شه — کنترل واقعی همیشه باید سمت سرور (توی query) انجام بشه.

## ۴. Routes که تغییر وضعیت می‌دن (POST)

- بعد از یک عملیات POST که باعث تغییر دائمی می‌شه (delete, logout, ...)، به‌جای render مستقیم، از `redirect` استفاده می‌شه تا از ارسال دوباره‌ی فرم با رفرش مرورگر (duplicate submission) جلوگیری بشه.
- استثنا: وقتی هدف کاربر وارد کردن پشت‌سرهم چند مورد باشه (مثل افزودن دسته‌ای کاربر)، می‌تونیم به‌جای redirect، همون صفحه رو دوباره render کنیم — ولی این تصمیم آگاهانه گرفته می‌شه، نه پیش‌فرض.
- Route هایی که فقط باعث تغییر وضعیت می‌شن (مثل logout) باید `POST`-only باشن (نه `GET`)، و از طریق فرم (نه لینک ساده `<a>`) فراخوانی بشن.

## ۵. Flash Messages

- هر `flash(message, category)` باید از یک category استاندارد استفاده کنه: `success`, `error`, `warning`, `info` (یا category های سفارشی تعریف‌شده مثل `post` در این پروژه).
- category هیچ‌وقت نباید شامل فاصله یا متن اضافه باشه — فقط یک کلمه‌ی تک که مستقیم به کلاس CSS نگاشت می‌شه.

## ۶. نام‌گذاری Relationship ها (SQLAlchemy)

- فیلدهای relationship با پیشوند `a_` نام‌گذاری می‌شن (مثل `a_user`, `a_posts`, `a_tags`) تا از فیلدهای معمولی ستون‌ها قابل‌تشخیص باشن.

## ۷. ساختار کلی فایل‌ها

- منطق route ها و HTTP handling: فایل اصلی اپ (`app.py`)
- منطق دیتابیس (مدل‌ها، query ها، توابع CRUD): فایل جدا (`database_test.py` یا معادلش)
- هیچ کوئری خام دیتابیسی مستقیم داخل route ها نوشته نمی‌شه — همیشه از طریق توابع لایه‌ی دیتابیس صدا زده می‌شه.

## ۸. ساختار کامنت‌های جداکننده

کامنت‌هایی که با خط تیره (`-`) نوشته می‌شن، برای دسته‌بندی سریع بخش‌های فایل و پیدا کردن موضوع موردنظر با یک نگاه یا جست‌وجو استفاده می‌شن. این کامنت‌ها ممکنه در آینده کوتاه‌تر یا دقیق‌تر بشن؛ چیزی که فعلاً اهمیت داره، حفظ منطق کلی دسته‌بندی و سطح اهمیت اون‌هاست.

### سطح اهمیت دسته‌های اصلی

- یک ستاره (`*`) برای دسته‌های اصلی معمولی استفاده می‌شه.
- دو ستاره (`**`) برای موضوعات کلی‌تر و مهم‌تر استفاده می‌شه؛ مثل لاگ یا دسته‌ای که چند بخش مرتبط رو پوشش می‌ده.
- سه ستاره (`***`) برای بخش‌های بسیار اصلی و حیاتی استفاده می‌شه؛ مثل مدیریت خطا که نبودنش می‌تونه عملکرد کلی برنامه رو مختل کنه.

تعداد ستاره‌ها فقط سطح اهمیت و کلی‌بودن دسته رو نشان می‌ده و به معنی اولویت اجرای کد نیست. برای زیربخش‌های یک دسته، معمولاً ستاره استفاده نمی‌شه تا تفاوت بین دسته‌ی اصلی و بخش‌های زیرمجموعه سریع مشخص باشه.

نمونه‌ی کلی:

<div dir="ltr">

```python
# ----------------** LOG **---------------------------
# ---------------- auth: login -----------------------

# ----------------* POSTS *---------------------------
# ---------------- posts: create ---------------------
# ---------------- posts: list -----------------------

# ---------------*** ERROR HANDLING ***---------------
# --------------- app: not_found ---------------------
```

</div>

### کامنت‌های مربوط به توابع

کامنت قبل از هر تابع باید کوتاه و کاربردی باشه و در سریع‌ترین نگاه این دو مورد رو مشخص کنه:

- تابع مربوط به چه موضوعی هست (مثل `auth`, `profile`, `posts` یا `tags`)
- کار اصلی تابع چیه (مثل `login`, `create`, `list`, `delete` یا `validate`)

لازم نیست نام کامل تابع عیناً در کامنت تکرار بشه؛ حتی بهتره کامنت یک توضیح کوتاه و معنایی باشه تا با جست‌وجوی نام تابع اشتباه نشه. هدف کامنت، شناسایی سریع نقش تابع و حوزه‌ی مربوط به اون هست، نه جایگزین شدن با نام تابع.

نمونه:

<div dir="ltr">

```python
# ---------------- auth: validate credentials ----------------
def check_login_data(data):
    ...

# ---------------- posts: create new entry ----------------
def save_post(user_id, title, content):
    ...
```

</div>

### نمادهای دیگر

نمادهایی غیر از ستاره، مثل `#`، فعلاً قرارداد قطعی ندارن و فقط می‌تونن به‌عنوان علامت شخصی برای تفاوت‌گذاری استفاده بشن. در حال حاضر نباید از شکل یا تعداد این نمادها برداشت رسمی درباره‌ی سطح اهمیت یا ساختار بخش داشت.

## ۹. استاندارد پیام‌های Commit

این پروژه از الگوی Conventional Commits همراه با scope استفاده می‌کنه:

<div dir="ltr">

```
type(scope): description
```

</div>

type یکی از این مقادیره: feat, fix, refactor, docs, chore, test
scope بخش مشخصی از پروژه‌ست که کامیت به آن مربوطه: auth, posts, profile, tags, db, config
اگه کامیت سطح کل ریپو باشه (مثل مستندات یا تنظیمات پایه، نه یک فیچر خاص)، scope گذاشته نمی‌شه.
پیام‌ها به انگلیسی، با فعل امری، و زیر ۵۰ کاراکتر برای خط اول نوشته می‌شن.

مثال‌ها:

<div dir="ltr">

```
docs: add initial README and project conventions
feat(auth): add user registration route
refactor(db): migrate from global session to scoped session
```

</div>

## ۱۰. فرمت لاگ‌ها (Lazy %s، نه f-string)

- تمام فراخوانی‌های `logger.*` (`info`, `warning`, `error`, `debug`) باید با فرمت
  lazy یعنی `%s` و آرگومان‌های جدا نوشته بشن، نه f-string:

  logger.info("User promoted (id=%s, username=%s)", user.id, user.username)

- استفاده از f-string، `.format()`، یا عملگر `%` برای ساختن رشته قبل از پاس دادن
  به logger ممنوعه — چون این کار ماهیت lazy رو از بین می‌بره (رشته حتی وقتی سطح
  لاگ اجازه‌ی نمایش پیام رو نمی‌ده هم ساخته می‌شه).
- این قانون فقط مخصوص لاگ‌هاست؛ سایر رشته‌های مقداردار (مثل خروجی `print()` که
  مستقیم به کاربر نشون داده می‌شه) همچنان f-string می‌مونن.

</div>
